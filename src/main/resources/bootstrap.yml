spring:
  application:
    name: data-management
  cloud:
    config:
      #      uri: http://localhost:8888
      username: admin
      password: admin
      discovery:
        service-id: config-service